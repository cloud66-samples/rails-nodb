production:
  first_thing:
    - command: apt-get install -y postgresql-client
      target: rails
      apply_during: build_only
    - command: sudo apt-get install libicu-dev --assume-yes
      target: rails
      apply_during: all
